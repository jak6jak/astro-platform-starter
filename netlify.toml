[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Deploy previews and branch deploys inherit the same settings
[context.deploy-preview]
  command = "npm run build"

[context.branch-deploy]
  command = "npm run build"
